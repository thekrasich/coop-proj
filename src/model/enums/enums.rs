#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ColorTheme {
    Dark,
    Light,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum CultureId {
    EnUS,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum MeshType {
    Triangle,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ElementsOrder {
    Linear,
}